Spectral serif section title with optional meta on the right.

```jsx
<SectionHeading meta="11:00 – 19:00">Программа дня</SectionHeading>
```

- `children`: title text (serif).
- `meta`: muted right-aligned label (time range, count). Omit for a plain heading.
